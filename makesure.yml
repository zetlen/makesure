# yaml-language-server: $schema=./makesure-schema.json
rules:
  - pattern: "package.json"
    rules:
      - actions:
          - urgency: 0
            template: |
              # A dependency has changed
              
              A dependency (besides the frequently-iterating "wasm" lib) has changed.

              ```diff
              {{diffText}}
              ```
        filters:
          - type: "jq"
            args:
              - .dependencies | del(.wasm)