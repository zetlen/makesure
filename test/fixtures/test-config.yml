concerns:
  test-concern:
    signals:
      - watch:
          include: 'package.json'
          type: 'jq'
          query: '.dependencies'
        report:
          type: 'handlebars'
          template: |
            # Test Report

            Found changes in dependencies.

            ```diff
            {{diffText}}
            ```
