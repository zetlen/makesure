checksets:
  - include: 'package.json'
    checks:
      - actions:
          - urgency: 1
            template: |
              # Test Report

              Found changes in dependencies.

              ```diff
              {{diffText}}
              ```
        filters:
          - type: 'jq'
            query: '.dependencies'
